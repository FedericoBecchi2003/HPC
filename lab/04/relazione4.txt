Federico Becchi 30-04-2026

Ho eseguito l'installazione del client VPN FORTINET senza alcun problema

Come client SSH utilizzo la shell zsh di MAC OS
(base) federicobecchi@Federicos-MacBook-Air Downloads % echo $SHELL
/bin/zsh

Autenticazione tra tutti i nodi del cluster
Eseguo i seguenti comandi sul cluster
Si creano una coppia di chiavi SSH con algoritmo RSA senza password  , salvate in ~/.ssh/
ssh-keygen -t rsa -P '' -f ~/.ssh/id_rsa
cat ~/.ssh/id_rsa.pub >> ~/.ssh/authorized_keys
chmod 0600 ~/.ssh/authorized_keys (cambio di permessi solo il propietario può leggere e scrivere)
User 1100 (4200) 
4 r-Read
2 w-Write
1 x - Execute
4+2 = 6
Group 0
Other 0

esito positivo

Ambiente HPC (OS)
[federico.becchi@ui01 ~]$ cat /etc/system-release
CentOS Linux release 7.9.2009 (Core)

Relazione 1: 
I risultati per CPU e GPU si trovano nella relazione 1

Relazione 2


lab/04/MatrixMul/matrimul.png

Considerazioni

Ci aspettano risultami migliori con le ottimizzazioni , 
la versione naive non utilizza nessuna parallellizazione o istruzione SIMD ( a parità di dimensione del problema)
Inoltre con la GPU si ottengono risultati molto più altri rispetto alle singole CPU
Questo viene spiegato dal fatto che le GPU hanno migliaia di core (CUDA cores ) e il problema di moltiplicazione
matriciale è adattato  a questa architettura poichè il calcolo della matrice risultante è indipendente dagli altri
Il confronto con la GPU nel PC locale non è possibile poichè non dispone di una GPU nvidia , è stato quindi necessario usare l'HPC d'ateneo


Network

band_lat.sh

ho aggiunto queste due righe:

# SSH rifiuta la connessione (mpirun usa SSH per lanciare i demoni remoti)
# con "Host key verification failed."
docker compose exec wn1 sh -c 'mkdir -p ~/.ssh && ssh-keyscan -H wn1 wn2 >> ~/.ssh/known_hosts 2>/dev/null'
docker compose exec wn2 sh -c 'mkdir -p ~/.ssh && ssh-keyscan -H wn1 wn2 >> ~/.ssh/known_hosts 2>/dev/null'

registrazione delle chiavi ssh nei rispettivi known_hosts altrimenti mpirun fallisce

esegue un test di bandwith attraverso mpirun

intra-nodo e inter-nodo

risultati pc
lab/02/network/results/PC/band.png
lab/02/network/results/PC/lat.png

risultati Cluster

viene eseguito band_lat.slurm sul cluster

sbatch band_lat.slum

lab/02/network/results/Cluster/band.png
lab/02/network/results/Cluster/lat.png


Considerazioni

Sul cluster i test inter-nodo vengono eseguiti attraverso Omnipath quindi ci si aspettano i risultati di 
latenza e larghezza di banda più veloci rispetto al PC , in cui la rete cluster viene simulata attraverso due container docker
Rispettavamente i test di banda e latenza sono equivalenti.